// TODO: uncomment when adding support for iDRAC
// import { SelectItem } from 'primeng/api';

// enum SharingApprovalMode {
//   Full = 'full',
//   ReadOnly = 'read',
//   Deny = 'deny',
// }

// namespace SharingApprovalMode {
//   export function getSelectItems(): SelectItem[] {
//     return Object.entries(SharingApprovalMode)
//     // Filter out properties that are not from enum values (like function `getSelectItems`).
//     .filter(([_, value]) => typeof value === 'string')
//       .map(([key, value]) => ({
//         label: key,
//         value,
//       }));
//   }
// }

// export { SharingApprovalMode };
