[package]
name = "jmux-proto"
version = "0.0.0"
authors = ["Devolutions Inc. <infos@devolutions.net>"]
edition = "2021"
publish = false

[dependencies]
bytes = "1.4.0"
smol_str = "0.2.0"

[dev-dependencies]
proptest = "1.2.0"
jmux-generators = { path = "../jmux-generators" }
