[package]
name = "jmux-proto"
version = "0.0.0"
authors = ["Devolutions Inc."]
edition = "2021"
publish = false

[dependencies]
bytes = "1.1.0"
smol_str = "0.1.21"

[dev-dependencies]
proptest = "1.0.0"
jmux-generators = { path = "../jmux-generators" }
