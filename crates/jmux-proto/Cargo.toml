[package]
name = "jmux-proto"
version = "0.0.0"
authors = ["Devolutions Inc. <infos@devolutions.net>"]
edition = "2021"
publish = false

[dependencies]
bytes = "1.5"
smol_str = "0.2"

[dev-dependencies]
proptest = "1.3"
jmux-generators = { path = "../jmux-generators" }
