[package]
name = "proxy-http"
version = "0.0.0"
authors = ["Devolutions Inc."]
edition = "2021"
description = "HTTP(S) proxy client and acceptor"
publish = false

[dependencies]
proxy-types = { path = "../proxy-types" }
tokio = { version = "1.14.0", features = ["io-util"] }

[dev-dependencies]
tokio = { version = "1.14.0", features = ["rt", "macros"] }
