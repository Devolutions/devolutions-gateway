use devolutions_pedm_shared::policy::ElevationResult;

/// The previous function was commented out.
/// 
/// The function call for `log_elevation` in `validate_elevation` still remains.
pub(crate) fn log_elevation(_res: &ElevationResult) -> anyhow::Result<()> {
    unimplemented!();
}
