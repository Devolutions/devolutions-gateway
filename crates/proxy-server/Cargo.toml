[package]
name = "proxy-server"
version = "0.0.0"
authors = ["Devolutions Inc. <infos@devolutions.net>"]
edition = "2021"
publish = false

[lints]
workspace = true

[dependencies]
proxy-socks = { path = "../proxy-socks" }
proxy-http = { path = "../proxy-http" }
proxy-types = { path = "../proxy-types" }
tokio = { version = "1.42", features = ["rt", "net", "rt-multi-thread", "macros"] }
