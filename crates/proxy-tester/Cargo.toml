[package]
name = "proxy-tester"
version = "0.0.0"
authors = ["Devolutions Inc."]
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1.17.0", features = ["rt", "net"] }
proxy-https = { path = "../proxy-https" }
proxy-socks = { path = "../proxy-socks" }
