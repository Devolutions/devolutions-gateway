# toolbox-changelog

Generates user-facing changelogs for Devolutions Gateway and Devolutions Agent in Devolutions ToolBox format from `CHANGELOG.md`.

## Prerequisites

No additional tools required beyond a working git repository.

Git is used to inspect commit diffs (`git show`) when a changelog entry needs more context to produce an accurate user-facing description.
