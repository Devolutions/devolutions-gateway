[workspace]
resolver = "2"
members = [
    "devolutions-gateway",
    "jetsocat",
    "crates/*",
    "tools/generate-openapi",
]
default-members = [
    "devolutions-gateway",
    "jetsocat",
]

[profile.production]
inherits = "release"
lto = true
