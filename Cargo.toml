[workspace]
members = [
    "devolutions-gateway",
    "jet-proto",
    "jetsocat",
    "jetsocat/proxy",
    "benchmark",
    "tools/*"
]
default-members = [
    "devolutions-gateway",
    "jet-proto",
    "jetsocat",
]

[patch.crates-io]
rustls = { git = "https://github.com/Devolutions/rustls.git", branch = "v0.18.1-patched", features = ["dangerous_configuration"] }
saphir = { git = "https://github.com/richerarc/saphir", rev = "1caa12d" } # FIXME: update once next saphir version is published

[profile.release]
lto = true

