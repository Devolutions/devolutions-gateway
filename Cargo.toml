[workspace]
resolver = "2"
members = [
    "devolutions-gateway",
    "jet-proto",
    "jetsocat",
    "jetsocat/proxy",
    "jmux-proto",
    "jmux-proxy",
    "benchmark",
    "tools/*"
]
default-members = [
    "devolutions-gateway",
    "jet-proto",
    "jetsocat",
]

[profile.production]
inherits = "release"
lto = true