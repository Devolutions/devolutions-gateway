[workspace]
resolver = "2"
members = [
    "devolutions-gateway",
    "jet-proto",
    "jetsocat",
    "jetsocat/proxy",
    "jmux-proto",
    "jmux-proxy",
    "tools/*",
    "crates/*",
]
default-members = [
    "devolutions-gateway",
    "jetsocat",
]

[profile.production]
inherits = "release"
lto = true
